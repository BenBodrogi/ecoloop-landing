import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800', '900'],
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'EcoLoop — Track Your Carbon. Build Better Habits.',
  description:
    'EcoLoop helps you measure your CO₂ footprint across transport, diet, energy, and purchases — then guides you to reduce it through daily habits, challenges, and community.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.variable}>{children}</body>
    </html>
  );
}
